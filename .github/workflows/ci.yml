name: CI

on: [push]

jobs:
  build:

    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Setup CI
      shell: cmd
      run: setup-ci
    - name: Build
      shell: cmd
      run: build
    - name: Test
      shell: cmd
      run: test
